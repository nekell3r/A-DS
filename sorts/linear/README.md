# Линейные сортировки O(n)

Специализированные алгоритмы сортировки с линейной сложностью. Работают только при определённых условиях и не основаны на сравнении элементов.

## ⚠️ Важно

Линейные сортировки **НЕ основаны на сравнении**. Они используют специфические свойства данных:
- Ограниченный диапазон значений
- Целочисленные значения
- Равномерное распределение

Для произвольных данных минимальная сложность сортировки через сравнения: **Ω(n log n)**

---

## 📁 Файлы

### counting.md - Сортировка подсчётом (Counting Sort)
**Сложность:** O(n + k), где k - диапазон значений  
**Память:** O(k)  
**Стабильная:** Да (при правильной реализации)

Подсчитывает количество каждого значения и восстанавливает отсортированный массив.

**Алгоритм:**
1. Находим min и max значения
2. Создаём массив счётчиков размером k = max - min + 1
3. Подсчитываем количество каждого значения
4. Восстанавливаем отсортированный массив

**Условия применения:**
- ✓ Целые числа
- ✓ Известный небольшой диапазон значений
- ✓ k ≈ n (диапазон сравним с количеством элементов)

**Примеры:**
- Сортировка оценок (1-5)
- Сортировка возрастов (0-120)
- Сортировка баллов теста (0-100)

**Когда НЕ использовать:**
- ✗ Диапазон очень большой (k >> n)
- ✗ Вещественные числа
- ✗ Строки

---

### radix.md - Поразрядная сортировка (Radix Sort)
**Сложность:** O(d × n), где d - количество разрядов  
**Память:** O(n + k)  
**Стабильная:** Да

Сортирует числа поразрядно, начиная с младшего или старшего разряда.

**Алгоритм (LSD - Least Significant Digit):**
1. Сортируем по младшему разряду (стабильной сортировкой)
2. Сортируем по следующему разряду
3. Повторяем для всех разрядов
4. После d проходов массив отсортирован

**Варианты:**
- **LSD** (Least Significant Digit) - справа налево
- **MSD** (Most Significant Digit) - слева направо

**Условия применения:**
- ✓ Числа фиксированной длины (или можно дополнить нулями)
- ✓ Количество разрядов d небольшое
- ✓ Есть стабильная O(n) сортировка для разрядов (обычно Counting Sort)

**Примеры:**
- Сортировка целых чисел в диапазоне [0, 999999]
- Сортировка дат (год-месяц-день)
- Сортировка IP-адресов
- Сортировка строк фиксированной длины

**Когда НЕ использовать:**
- ✗ Числа с очень разной длиной
- ✗ Вещественные числа (проблемы с разрядами)

---

### bucket.md - Блочная сортировка (Bucket Sort)
**Сложность:** O(n + k) в среднем, O(n²) в худшем  
**Память:** O(n + k)  
**Стабильная:** Зависит от реализации

Распределяет элементы по "блокам" (buckets), сортирует каждый блок отдельно и объединяет.

**Алгоритм:**
1. Создаём k пустых блоков
2. Распределяем элементы по блокам (по диапазонам значений)
3. Сортируем каждый блок отдельно (любым алгоритмом)
4. Объединяем отсортированные блоки

**Условия применения:**
- ✓ Данные равномерно распределены
- ✓ Известен диапазон значений
- ✓ Можно эффективно определить номер блока

**Примеры:**
- Сортировка вещественных чисел в [0, 1)
- Сортировка по первой букве (26 блоков)
- Внешняя сортировка с разделением по диапазонам

**Когда НЕ использовать:**
- ✗ Неравномерное распределение (все элементы попадут в один блок)
- ✗ Неизвестен диапазон значений

---

## 📊 Сравнение линейных алгоритмов

| Алгоритм | Сложность | Память | Условия | Стабильная |
|----------|-----------|--------|---------|------------|
| Counting Sort | O(n + k) | O(k) | Малый диапазон k | Да |
| Radix Sort | O(d × n) | O(n + k) | Фиксированная длина d | Да |
| Bucket Sort | O(n + k) | O(n + k) | Равномерное распределение | Зависит |

Где:
- **n** - количество элементов
- **k** - диапазон значений
- **d** - количество разрядов

---

## 🎯 Когда использовать линейные сортировки?

### Counting Sort
```
✓ Используйте:
  - Целые числа в небольшом диапазоне
  - k = O(n) или k < n
  - Например: возраст [0-120], оценки [1-5]

✗ Не используйте:
  - k >> n (k = миллион, n = 100)
  - Вещественные числа
```

### Radix Sort
```
✓ Используйте:
  - Числа фиксированной длины
  - d небольшое (d ≤ 10)
  - Например: 32-битные числа (d=32 бита)

✗ Не используйте:
  - Очень разная длина чисел
  - d очень большое
```

### Bucket Sort
```
✓ Используйте:
  - Равномерно распределённые данные
  - Знаем диапазон заранее
  - Например: случайные float в [0,1)

✗ Не используйте:
  - Неравномерное распределение
  - Неизвестен диапазон
```

---

## 💡 Примеры применения

### Counting Sort - оценки студентов
```c
// Оценки от 2 до 5
int grades[] = {4, 5, 3, 4, 5, 2, 3, 4, 5};
// O(n + k) где k=4, очень быстро!
```

### Radix Sort - телефонные номера
```c
// Все номера 10 цифр
int phones[] = {9001234567, 9007654321, ...};
// O(10 × n) = O(n), 10 проходов по разрядам
```

### Bucket Sort - процентили
```c
// Результаты теста от 0.0 до 100.0
float scores[] = {85.5, 92.3, 78.1, ...};
// Создаём 10 блоков: [0-10), [10-20), ..., [90-100]
```

---

## 📈 Производительность

### Теоретическая:

| n | k | Counting | Radix (d=8) | Quick Sort |
|---|---|----------|-------------|------------|
| 1000 | 100 | **1100** | **8000** | 10000 |
| 10000 | 1000 | **11000** | **80000** | 130000 |
| 100000 | 10000 | **110000** | **800000** | 1700000 |

### Практическая:
- Линейные сортировки быстрее **только при соблюдении условий**
- Quick Sort обычно быстрее на практике (лучше кэш, меньше операций)
- Линейные имеют большие константы

---

## ⚙️ Гибридные подходы

### Radix Sort + Insertion Sort
```c
// Для малых блоков используем Insertion Sort
if (bucket_size < 10) {
    insertion_sort(bucket);
} else {
    radix_sort(bucket);
}
```

### Bucket Sort + Quick Sort
```c
// Сортируем каждый блок Quick Sort
for each bucket:
    quick_sort(bucket);
```

---

## 🔗 Применение в реальности

### Counting Sort:
- Сортировка пикселей изображения (0-255)
- Подсчёт голосов на выборах
- Гистограммы

### Radix Sort:
- Сортировка строк (MSD Radix)
- Suffix Array construction
- Integer sorting в некоторых языках

### Bucket Sort:
- Внешняя сортировка с разделением
- MapReduce shuffle фаза
- Распределённые системы

---

## 🎓 Важные замечания

1. **Не основаны на сравнении** - обходят барьер Ω(n log n)

2. **Требуют условий:**
   - Counting: малый k
   - Radix: малый d
   - Bucket: равномерное распределение

3. **На практике:**
   - Quick Sort часто быстрее (несмотря на O(n log n))
   - Большие константы в линейных алгоритмах
   - Проблемы с кэшем

4. **Стабильность важна:**
   - Radix Sort работает только со стабильной сортировкой разрядов
   - Counting Sort должен быть реализован стабильно

---

## 🔗 См. также

- `../simple/` - простые O(n²) алгоритмы
- `../advanced/` - эффективные O(n log n) алгоритмы
- `../external/` - внешние сортировки
