# Внешние сортировки

Алгоритмы для сортировки данных, которые не помещаются в оперативную память (например, большие файлы на диске).

## 📁 Файлы

### merge.c - Сортировка слиянием (Merge Sort)
**Сложность:** O(n log n) всегда  
**Память:** O(n)  
**Стабильная:** Да

Классическая сортировка слиянием по алгоритму "разделяй и властвуй".

**Алгоритм:**
1. Если массив содержит один элемент - возвращается (уже отсортирован)
2. Разбиваем массив на две примерно равные части
3. Каждая часть рекурсивно сортируется
4. Отсортированные части сливаются в порядке возрастания
5. Получившийся массив возвращается

**Особенности:**
- ✓ Гарантированная O(n log n) всегда
- ✓ Стабильная сортировка
- ✓ Предсказуемая производительность
- ✓ Хорошо подходит для связных списков
- ✓ Легко распараллеливается
- ✗ Требует O(n) дополнительной памяти

**Когда использовать:**
- Большие файлы (внешняя сортировка)
- Когда важна стабильность
- Связные списки
- Параллельная сортировка

---

### natural_merge.c - Естественная сортировка слиянием (Natural Merge Sort)
**Сложность:** O(n log n) в худшем, O(n) в лучшем  
**Память:** O(n)  
**Стабильная:** Да

Адаптивная версия сортировки слиянием, использующая естественные серии (уже отсортированные участки).

**Алгоритм:**
1. **Разделение:** находим все естественные серии в массиве
   - Серия = максимальная подпоследовательность в неубывающем порядке
   - Например: `[1,3,5, 2,4,6, 7,8]` → 3 серии
2. **Слияние:** объединяем пары соседних серий
3. **Повторение:** повторяем до получения одной серии (весь массив отсортирован)

**Естественная серия:**
```
arr[i] <= arr[i+1] <= arr[i+2] <= ... (неубывающая последовательность)
```

**Особенности:**
- ✓ Адаптивная: использует существующий порядок
- ✓ O(n) для уже отсортированных данных
- ✓ Эффективна для частично отсортированных данных
- ✓ Хорошо подходит для внешней сортировки
- ✗ Требует два прохода на каждой итерации (поиск серий + слияние)

**Примеры серий:**
- `[1,2,3,4,5]` → 1 серия (уже отсортирован) ⭐ O(n)
- `[1,3,5, 2,4,6]` → 2 серии
- `[5,4,3,2,1]` → 5 серий (каждый элемент отдельная серия) - худший случай

---

## 📊 Сравнение

| Характеристика | Merge Sort | Natural Merge Sort |
|----------------|------------|---------------------|
| Лучший случай | O(n log n) | **O(n)** ⭐ |
| Средний случай | O(n log n) | O(n log n) |
| Худший случай | O(n log n) | O(n log n) |
| Память | O(n) | O(n) |
| Стабильная | ✓ | ✓ |
| Адаптивная | ✗ | ✓ |
| Проходов | log n | log n (или меньше) |

---

## 🎯 Когда что использовать?

### Merge Sort (merge.c)
**Используйте когда:**
- ✓ Нужна гарантированная O(n log n)
- ✓ Важна стабильность
- ✓ Данные произвольные (нет существующего порядка)
- ✓ Внешняя сортировка файлов
- ✓ Параллельная обработка

**Примеры:**
- Сортировка больших файлов
- Многопоточная сортировка
- Сортировка связных списков

### Natural Merge Sort (natural_merge.c)
**Используйте когда:**
- ✓ Данные частично отсортированы
- ✓ Есть естественные серии
- ✓ Важна адаптивность
- ✓ Внешняя сортировка с упорядоченными участками

**Примеры:**
- Логи с временными метками
- Данные от нескольких источников
- Инкрементальные обновления

---

## 💾 Внешняя сортировка

### Принцип работы:

1. **Фаза 1: Разделение**
   - Читаем файл порциями, помещающимися в память
   - Сортируем каждую порцию в памяти
   - Записываем отсортированные порции во временные файлы

2. **Фаза 2: Слияние**
   - Сливаем временные файлы попарно
   - Повторяем до получения одного файла

### Пример для файла 1GB при 100MB памяти:

```
Исходный файл: 1GB

Шаг 1: Разделение
→ 10 файлов по 100MB (каждый отсортирован)

Шаг 2: Слияние пар
→ 5 файлов по 200MB

Шаг 3: Слияние пар
→ 3 файла (200MB + 200MB + 600MB)

Шаг 4: Слияние
→ 1 файл (600MB + 400MB)

Итого: 1GB отсортированный файл
```

### Оптимизации:

- **K-way merge** - сливаем k файлов одновременно (не только пары)
- **Буферизация** - используем буферы для чтения/записи
- **Natural runs** - используем естественные серии (Natural Merge Sort)
- **Replacement Selection** - создаём более длинные начальные серии

---

## 🔧 Реализация внешней сортировки

Для работы с файлами нужно:

```c
// 1. Разделение на части
FILE* split_file(const char* filename, size_t chunk_size);

// 2. Сортировка части в памяти
void sort_chunk(int* chunk, size_t size);

// 3. Слияние двух файлов
FILE* merge_files(FILE* f1, FILE* f2);

// 4. Основной цикл
void external_merge_sort(const char* input) {
    // Разделяем на отсортированные части
    FILE** chunks = split_and_sort(input);
    
    // Сливаем пока не останется один файл
    while (count > 1) {
        chunks = merge_pairs(chunks, count);
        count = count / 2;
    }
}
```

---

## 📈 Производительность

### В памяти (массивы):
- **Merge Sort**: стабильная, предсказуемая O(n log n)
- **Natural Merge**: быстрее на частично отсортированных

### На диске (файлы):
- **Merge Sort**: классический выбор для внешней сортировки
- **Natural Merge**: эффективнее при естественных сериях
- **K-way Merge**: меньше проходов по диску

---

## 💡 Интересные факты

- Merge Sort изобретён в 1945 году Джоном фон Нейманом
- Natural Merge Sort особенно эффективна для сортировки магнитных лент
- Современные базы данных используют варианты внешнего слияния
- TimSort (Python, Java) основан на Natural Merge Sort

---

## 🔗 См. также

- `../advanced/` - быстрые алгоритмы для данных в памяти
- `../simple/` - простые алгоритмы O(n²)
- Структуры данных: очередь с приоритетом для k-way merge
