# Продвинутые сортировки O(n log n)

Эффективные алгоритмы сортировки для больших массивов. Все имеют логарифмическую или близкую к ней сложность.

## 📁 Файлы

### heap.c - Пирамидальная сортировка (Heap Sort)
**Сложность:** O(n log n) всегда  
**Память:** O(1) in-place  
**Стабильная:** Нет

Преобразует массив в структуру "куча" (max-heap), затем последовательно извлекает максимумы.

**Алгоритм:**
1. Строим кучу для всех непустых подмассивов
2. Перемещаем элементы вверх по структуре кучи
3. Извлекаем максимум и восстанавливаем кучу

**Особенности:**
- ✓ Гарантированная O(n log n) в любом случае
- ✓ In-place (не требует дополнительной памяти)
- ✗ Плохая локальность кэша (медленнее Quick Sort на практике)
- Используется в Priority Queue

**Индексы:**
- Левый потомок: `2*i + 1`
- Правый потомок: `2*i + 2`
- Родитель: `(i-1) / 2`

---

### hoar_rec.c - Быстрая сортировка Хоара (рекурсивная)
**Сложность:** O(n log n) в среднем, O(n²) в худшем  
**Память:** O(log n) для стека рекурсии  
**Стабильная:** Нет

Рекурсивный алгоритм "разделяй и властвуй". Выбирает опорный элемент и разбивает массив на две части.

**Алгоритм:**
1. Выбираем опорный элемент (pivot)
2. Разбиваем массив: меньшие слева, большие справа
3. Рекурсивно сортируем обе части

**Особенности:**
- ⭐ **Самая быстрая сортировка на практике**
- В среднем O(n log n)
- Худший случай O(n²) на отсортированных данных
- Использует рекурсию

---

### hoar_nonrec.c - Быстрая сортировка (нерекурсивная)
**Сложность:** O(n log n) в среднем, O(n²) в худшем  
**Память:** O(log n) для явного стека  
**Стабильная:** Нет

Нерекурсивная реализация Quick Sort с использованием явного стека.

**Особенности:**
- Избегает переполнения стека
- Явное управление памятью
- Можно оптимизировать порядок обработки

---

### shell.c - Сортировка Шелла
**Сложность:** O(n^1.5) в среднем, зависит от последовательности шагов  
**Память:** O(1)  
**Стабильная:** Нет

Обобщение сортировки вставками. Сравнивает элементы на расстоянии gap, постепенно уменьшая это расстояние.

**Алгоритм:**
1. Начинаем с большого шага (gap)
2. Сортируем вставками элементы на расстоянии gap
3. Уменьшаем gap (обычно gap = gap/2)
4. Повторяем до gap = 1

**Особенности:**
- Эффективна для средних массивов (1000-10000)
- Простая реализация
- Производительность зависит от последовательности шагов
- Сложность между O(n log n) и O(n²)

---

### tournament.c - Турнирная сортировка (Tournament Sort)
**Сложность:** O(n log n) всегда  
**Память:** O(n) для турнирного дерева  
**Стабильная:** Нет

Использует турнирное дерево (дерево выбора) для эффективного поиска минимума.

**Алгоритм:**
1. Строим турнирное дерево из элементов массива
2. Корень дерева = минимальный элемент
3. Извлекаем минимум, заменяем на "бесконечность"
4. Обновляем дерево и повторяем

**Особенности:**
- Улучшенная версия Selection Sort
- Каждое сравнение дает больше информации
- Хорошо для слияния k отсортированных последовательностей
- Используется во внешней сортировке

**Структура:**
- Полное бинарное дерево: 2n-1 узлов
- Листья: исходные элементы
- Родители: минимум из детей

---

### smoothsort.md - Плавная сортировка
Описание алгоритма Smoothsort (усовершенствованная Heap Sort).

---

## 📊 Сравнение продвинутых алгоритмов

| Алгоритм | Лучший | Средний | Худший | Память | In-place | Стабильная |
|----------|--------|---------|--------|--------|----------|------------|
| Heap Sort | O(n log n) | O(n log n) | **O(n log n)** ⭐ | O(1) | ✓ | ✗ |
| Quick Sort | O(n log n) | **O(n log n)** ⭐ | O(n²) | O(log n) | ✓ | ✗ |
| Merge Sort | O(n log n) | O(n log n) | O(n log n) | O(n) | ✗ | ✓ |
| Shell Sort | O(n log n) | O(n^1.5) | O(n²) | O(1) | ✓ | ✗ |
| Tournament Sort | O(n log n) | O(n log n) | O(n log n) | O(n) | ✗ | ✗ |

## 🎯 Когда что использовать?

### Quick Sort (hoar_rec.c, hoar_nonrec.c)
**Используйте когда:**
- ✓ Нужна максимальная скорость в среднем случае
- ✓ Можно рискнуть с O(n²) в худшем случае
- ✓ Стабильность не важна

**Не используйте когда:**
- ✗ Нужна гарантия O(n log n)
- ✗ Данные могут быть уже отсортированы (без рандомизации pivot)

### Heap Sort (heap.c)
**Используйте когда:**
- ✓ Нужна гарантированная O(n log n)
- ✓ Ограничена память (in-place)
- ✓ Стабильность не важна

**Не используйте когда:**
- ✗ Нужна максимальная скорость (Quick Sort быстрее)
- ✗ Важна локальность кэша

### Shell Sort (shell.c)
**Используйте когда:**
- ✓ Средние массивы (1000-10000 элементов)
- ✓ Нужна простая реализация O(n log n)
- ✓ Ограничена память

**Не используйте когда:**
- ✗ Большие массивы (> 100k) - Quick/Merge Sort эффективнее

### Tournament Sort (tournament.c)
**Используйте когда:**
- ✓ Нужно слить k отсортированных последовательностей
- ✓ Внешняя сортировка (файлы)
- ✓ Каждое сравнение должно давать максимум информации

**Не используйте когда:**
- ✗ Простая сортировка в памяти (Quick/Heap Sort лучше)

---

## 💡 Оптимизации

### Для Quick Sort:
- Случайный выбор pivot
- Медиана трех элементов как pivot
- Переход на Insertion Sort для малых подмассивов (< 10)
- Трёхсторонее разбиение для дубликатов

### Для Heap Sort:
- Bottom-up heapify
- Оптимизация кэша

### Для Shell Sort:
- Последовательность Кнута: h(k+1) = 3*h(k) + 1
- Последовательность Седжвика
- Последовательность Пратта

---

## 📈 Практическая производительность

На реальных данных (средний случай):

1. **Quick Sort** - самая быстрая (кэш-дружественная)
2. **Merge Sort** - стабильная альтернатива
3. **Heap Sort** - гарантии без доп. памяти
4. **Shell Sort** - простая и неплохая
5. **Tournament Sort** - специализированная

---

## 🔗 Гибридные алгоритмы

Современные библиотечные реализации используют гибриды:

- **IntroSort** = Quick Sort + Heap Sort (C++ std::sort)
- **TimSort** = Merge Sort + Insertion Sort (Python, Java)
- **pdqsort** = Quick Sort + Heap Sort + Insertion Sort

---

## 🔗 См. также

- `../simple/` - простые O(n²) алгоритмы
- `../external/` - для данных на диске
- `../linear/` - O(n) для специальных случаев
